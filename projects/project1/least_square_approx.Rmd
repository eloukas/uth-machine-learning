---
title: "Least Squares Approximation"
output: html_notebook
---



```{r}
# Init environment and read data
library("MASS")
data = read.table("data.txt")
data = as.matrix(data)

dimensions = dim(data) 
```

# Exercise 1
# Least Square Approximation
```{r}
source("least_square_algorithm.R", local=environment())
```

# Square Test
```{r}
source("multiple_Rsquare.R", local=environment())
```

# Exercise 2
# Generalization and Visualization
```{r}
source("generalization.R", local=environment())
```
# Exercise 3
# Cross-validation
```{r}
source("cross_validation.R", local=environment())
```
# Exercise 4:
# Recursive Least-Squares
```{r}
source("rls.R", local=environment())
```